# Quakey

Quakey is a Deterministic Simulation Testing framework for C. You can use it to test distributed applications with fault injections in a completely deterministic environment. It works by essentially mocking system calls via preprocessor substitution to use in-memory disk and networking and run all programs in the same process.

